# Copy this file to config.env and fill in your values.
# config.env is NOT committed to git (contains secrets).

# ── LTE Modem (Quectel EC25/EG-25) ──────────────────────────────────────────
APN=gprs.swisscom.ch
MODEM_USER=gprs
MODEM_PASS=gprs

# ── AutoSSH Reverse Tunnel ───────────────────────────────────────────────────
# Each device gets its own port on the jump host.
# Ikarus-1 = 2010, Ikarus-2 = 2011, etc.
TUNNEL_HOST=192.33.89.14
TUNNEL_USER=pi
TUNNEL_PORT=2010
SSH_KEY=/home/pi/.ssh/luckyluke

# ── GNSS (u-blox) ────────────────────────────────────────────────────────────
# Serial device: /dev/ttyUSB0 or /dev/serial0 (GPIO UART)
GNSS_DEVICE=/dev/ttyUSB0
GNSS_BAUD=9600

# ── Data Logging ─────────────────────────────────────────────────────────────
LOG_DIR=/home/pi/aware-pi-logger/logs
LOG_INTERVAL_SEC=1
